# Server Configuration
PORT=8080

# Database Configuration
DB_DIALECT=sqlite
DB_DSN=cupcake_store.db

# For PostgreSQL (production)
# DB_DIALECT=postgres
# DB_DSN=host=localhost user=cupcake_user password=cupcake_pass dbname=cupcake_store port=5432 sslmode=disable

# Log Configuration
LOG_LEVEL=info

